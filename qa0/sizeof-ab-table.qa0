(verbose [(c99 bgl/xlc) "/* $Id$ */"])

(include "types.qa0")

(procedure size-of-ab-table ([stem "sizeof_ABTable"]
	                     [return total int "int"])
	   ([l-s    int "Ls"     "int"])
   (op int-mul () (total) ([reg l-s] [const (size-of AB-Table)])))
