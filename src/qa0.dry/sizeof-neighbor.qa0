(verbose [c99 "/* QA0 source $Id$ */"])

(include "types.qa0")

(procedure size-of-neighbor ([stem "sizeof_neighbor"]
	                     [return total int "int"])
	   ([volume    int "volume"     "int"])
   (op int-mul () (total) ([reg volume] [const (size-of neighbor)])))
