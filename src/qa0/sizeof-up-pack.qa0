(verbose [(c99 bgl/xlc) "/* $Id$ */"])

(include "types.qa0")

(procedure size-up-pack ([stem "sizeof_up_pack"]
	          	 [return total int "int"])
           ([volume int "volume" "int"])
   (op int-mul () (total) ([reg volume] [const (size-of up-pack)])))
